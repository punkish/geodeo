{
  "name": "geodeo",
  "scripts": {
    "dev": "bun run --hot ./src/index.js",
    "compile": "bun build --compile --target=bun-linux-x64-modern ./src/index.js --outfile ./bin/geodeox"
  },
  "dependencies": {
    "geojson-vt": "^4.0.2",
    "vt-pbf": "^3.1.3"
  },
  "type": "module",
  "devDependencies": {
    "@types/bun": "latest"
  }
}
